# TASK — тикет

Role: TESTER
TicketId: T0005
CreatedAt: 2026-04-10 22:26

## Контекст

`CODER` завершил реализацию Rider-плагина и отдельно убрал привязку action к абсолютному пути репозитория. По отчёту `T0004` вычисление `path` теперь идёт относительно текущего проекта через `project.basePath`, а минимальная сборочная проверка после настройки Java завершилась успешно.

Нужно провести сценарную проверку поведения action в рамках спецификации `T0002` и подтвердить, что фактическое поведение соответствует ожидаемому формату и ограничениям.

Проверка должна оставаться в scope текущего репозитория `D:\.DEV\JetBrainsPlagins`, без правок кода и без расширения задачи.

## Scope

- Проверить action `CopySelectionWithPathAndLinesAction` как пользовательский сценарий Rider-плагина.
- Валидировать формат буфера обмена по спецификации `T0002`.
- Проверить сценарии для файлов внутри текущего репозитория.
- Проверить защитные сценарии, где буфер обмена не должен изменяться.
- Зафиксировать подтверждённые результаты и воспроизводимость по каждому сценарию.

## Не делать

- Не исправлять код и не менять реализацию плагина.
- Не расширять тестирование на несвязанные функции, которых нет в текущем scope.
- Не менять файлы вне `D:\.DEV\JetBrainsPlagins`.
- Не подменять сценарную проверку предположениями, если реальная проверка невозможна; в таком случае явно фиксировать блокер.

## Входные артефакты

- отчёт ANALYST: `.agents-runtime/MAIL/ANALYST/OUT/T0002_20260410-0130_ANALYST_specify-rider-plugin-copy-format-bootstrap_report.md`
- отчёт CODER по первичной реализации: `.agents-runtime/MAIL/CODER/OUT/T0003_20260410-0205_CODER_bootstrap-rider-plugin-and-implement-copy-action_report.md`
- отчёт CODER по отвязке от абсолютного пути: `.agents-runtime/MAIL/CODER/OUT/T0004_20260410-2115_CODER_remove-absolute-root-and-make-path-resolution-project-relative_report.md`

## Шаги

1. Прочитать спецификацию `T0002` и оба отчёта `CODER`.
2. Подготовить минимальный набор сценариев, покрывающий позитивные и защитные кейсы.
3. Проверить результат копирования для валидного выделения внутри файла репозитория.
4. Проверить кейсы пустого выделения, нефайловых/служебных вкладок, несохранённых вкладок и файлов вне репозитория.
5. Сопоставить ожидаемый и фактический результат по каждому сценарию.
6. Подготовить OUT-отчёт с явным разделением: подтверждено, не подтверждено, заблокировано инфраструктурой.

## Проверки

- Проверить, что для валидного выделения внутри файла проекта формат буфера ровно такой:
  - строка 1: `<path>:<lineStart>-<lineEnd>`
  - строка 2+: выделенный текст
- Проверить, что `path` относительный и использует `/`.
- Проверить, что `lineStart` и `lineEnd` соответствуют правилам `T0002`, включая случай с завершающим `\n`.
- Проверить, что пустое выделение не меняет буфер обмена.
- Проверить, что несохранённые/служебные/нефайловые вкладки не меняют буфер обмена.
- Проверить, что файл вне текущего проекта/репозитория не обрабатывается.
- Если полноценная ручная проверка в Rider невозможна в текущем окружении, явно описать, какой именно шаг заблокирован и что удалось проверить вместо этого.

## Критерии готовности

- [ ] Подготовлен набор сценариев, покрывающий ключевые требования `T0002`.
- [ ] Для каждого сценария зафиксированы шаги, ожидаемый и фактический результат.
- [ ] Подтверждено или опровергнуто соответствие текущей реализации спецификации.
- [ ] Явно зафиксированы блокеры, если они мешают полной проверке.
- [ ] Подготовлен OUT-отчёт как вход для REVIEWER.

## Артефакты OUT

- отчёт TESTER: `.agents-runtime/MAIL/TESTER/OUT/T0005_20260410-2226_TESTER_validate-copy-action-scenarios_report.md`

## Рекомендованная модель и режим

- model: gpt-5
- reasoning effort: medium
- комментарий: если часть сценариев придётся подтверждать косвенно из-за ограничений запуска Rider, повышать до high
