# TASK — тикет

Role: CODER
TicketId: T0004
CreatedAt: 2026-04-10 21:15

## Контекст

В репозитории уже поднят минимальный Rider-плагин и реализована action копирования выделения. Следующий шаг: убрать зависимость реализации от захардкоженного абсолютного пути и привести вычисление корня/относительного `path` к модели, которая не завязана на конкретную рабочую директорию машины.

Сейчас в реализации есть привязка к абсолютному пути репозитория, из-за чего поведение может ломаться при переносе проекта или запуске из другого каталога. Это противоречит цели спецификации `T0002`, где формат должен оставаться корректным независимо от физического расположения репозитория.

Цель тикета: сохранить текущую функциональность action, но заменить абсолютную привязку на вычисление пути относительно текущего проекта/репозитория средствами IDE или другой надёжной локальной точки отсчёта внутри scope этого репозитория.

## Scope

- Изменить реализацию action так, чтобы она не зависела от захардкоженного абсолютного пути репозитория.
- Использовать относительный `path` от текущего корня проекта/репозитория, определяемого во время выполнения.
- Сохранить формат результата без изменений:
  - строка 1: `<path>:<lineStart>-<lineEnd>`
  - строка 2+: выделенный текст
- Сохранить нормализацию `path` к `/`.
- Сохранить правила вычисления `lineStart` и `lineEnd` по спецификации `T0002`.
- Сохранить защитное поведение:
  - пустое выделение не меняет буфер обмена;
  - несохранённые вкладки не меняют буфер обмена;
  - служебные/нефайловые вкладки не меняют буфер обмена;
  - файлы вне текущего проекта/репозитория не меняют буфер обмена.
- Если для вычисления корня доступны несколько API IDE, выбрать минимально достаточный и устойчивый вариант без расширения scope.

## Не делать

- Не добавлять новые пользовательские функции, UI, настройки или альтернативные форматы копирования.
- Не менять формат буфера обмена, уже зафиксированный в `T0002`.
- Не ослаблять ограничение на файлы вне текущего проекта/репозитория.
- Не менять файлы вне `D:\.DEV\JetBrainsPlagins`.
- Не переписывать проект заново, если достаточно локальной правки текущей реализации.

## Входные артефакты

- отчёт ANALYST: `.agents-runtime/MAIL/ANALYST/OUT/T0002_20260410-0130_ANALYST_specify-rider-plugin-copy-format-bootstrap_report.md`
- предыдущий тикет CODER: `.agents-runtime/MAIL/CODER/IN/T0003_20260410-0205_CODER_bootstrap-rider-plugin-and-implement-copy-action.md`
- предыдущий отчёт CODER: `.agents-runtime/MAIL/CODER/OUT/T0003_20260410-0205_CODER_bootstrap-rider-plugin-and-implement-copy-action_report.md`

## Шаги

1. Прочитать `T0002` и текущую реализацию action.
2. Найти место, где используется абсолютный путь, и заменить его на вычисление корня проекта/репозитория во время выполнения.
3. Убедиться, что относительный `path` формируется только для файлов внутри текущего проекта/репозитория.
4. Сохранить текущее поведение по строкам, выделению и нормализации перевода строк.
5. Прогнать минимально возможные проверки и честно зафиксировать результат.

## Проверки

- Проверить, что в коде больше нет привязки к конкретному абсолютному пути репозитория.
- Проверить, что action по-прежнему формирует относительный `path` с `/`.
- Проверить, что файлы вне текущего проекта/репозитория не обрабатываются.
- Проверить, что пустое выделение и служебные/несохранённые вкладки не меняют буфер обмена.
- Проверить, что проект проходит минимальную проверку сборки через Gradle Wrapper.
- Если проверка сборки не запускается из-за отсутствия `JAVA_HOME`/`java` или другого внешнего инфраструктурного ограничения, явно зафиксировать это в отчёте, не выдавая за успешную сборку.

## Критерии готовности

- [ ] Абсолютная привязка к пути репозитория убрана из реализации.
- [ ] `path` вычисляется относительно текущего проекта/репозитория во время выполнения.
- [ ] Формат копирования полностью соответствует `T0002`.
- [ ] Защитные ограничения поведения сохранены.
- [ ] Подготовлен OUT-отчёт как вход для TESTER.

## Артефакты OUT

- отчёт CODER: `.agents-runtime/MAIL/CODER/OUT/T0004_20260410-2115_CODER_remove-absolute-root-and-make-path-resolution-project-relative_report.md`

## Рекомендованная модель и режим

- model: gpt-5
- reasoning effort: medium
- комментарий: если выбор API корня проекта в IntelliJ Platform окажется неоднозначным, повышать до high
