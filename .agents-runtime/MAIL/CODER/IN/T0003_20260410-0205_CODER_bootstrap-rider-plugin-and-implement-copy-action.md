# TASK — тикет

Role: CODER
TicketId: T0003
CreatedAt: 2026-04-10 02:05

## Контекст

Нужно реализовать минимально достаточный Rider-плагин в каталоге `D:\!_DEV_!\JetBrainsPlagins` по спецификации из аналитического отчёта `.agents-runtime/MAIL/ANALYST/OUT/T0002_20260410-0130_ANALYST_specify-rider-plugin-copy-format-bootstrap_report.md`.

Цель реализации: из редактора Rider копировать в буфер обмена текст в формате:

1. `<path>:<lineStart>-<lineEnd>`
2. выделенный текст

Реализацию вести на Kotlin. Сборку и запуск проекта оформить через Gradle Wrapper внутри репозитория, без глобальной установки Kotlin или Gradle.

## Scope

- Создать минимальный Gradle-проект JetBrains/Rider plugin внутри `D:\!_DEV_!\JetBrainsPlagins`.
- Использовать Kotlin как язык реализации.
- Добавить Gradle Wrapper в проект.
- Реализовать action плагина, которая:
  - работает только для файлов внутри `D:\!_DEV_!\JetBrainsPlagins`;
  - получает относительный `path` с `/` как разделителем;
  - вычисляет `lineStart` и `lineEnd` по правилам из `T0002`;
  - копирует в буфер обмена заголовок и выделенный текст в точном формате из спецификации;
  - не изменяет буфер обмена для пустого выделения;
  - не изменяет буфер обмена для несохранённых вкладок, служебных вкладок и файлов вне `D:\!_DEV_!\JetBrainsPlagins`.
- Добавить минимально достаточные plugin-метаданные и структуру проекта, чтобы проект собирался.

## Не делать

- Не расширять функциональность сверх описанной операции копирования.
- Не добавлять поддержку альтернативных форматов, меню настроек, UI-экранов и дополнительных команд.
- Не менять файлы вне `D:\!_DEV_!\JetBrainsPlagins`.
- Не требовать глобальной установки Kotlin или Gradle на машину пользователя.

## Входные артефакты

- тикет LEAD: `.agents-runtime/MAIL/LEAD/IN/T0001_20260410-0108_LEAD_rider-plugin-bootstrap.md`
- отчёт ANALYST: `.agents-runtime/MAIL/ANALYST/OUT/T0002_20260410-0130_ANALYST_specify-rider-plugin-copy-format-bootstrap_report.md`

## Шаги

1. Прочитать входные артефакты и строго следовать спецификации `T0002`.
2. Создать минимальный plugin-проект на Kotlin с Gradle Wrapper.
3. Реализовать action копирования по спецификации.
4. Подключить action к plugin-метаданным так, чтобы проект был готов к дальнейшей проверке.
5. Прогнать минимальные проверки сборки и зафиксировать их результат в отчёте.

## Проверки

- Проверить, что в репозитории появились `gradlew`, `gradlew.bat`, wrapper-файлы и build-скрипты проекта.
- Проверить, что проект собирается через Gradle Wrapper.
- Проверить, что реализация явно учитывает ключевые ограничения из `T0002`:
  - только файлы внутри `D:\!_DEV_!\JetBrainsPlagins`;
  - пустое выделение не меняет буфер обмена;
  - несохранённые и служебные вкладки не меняют буфер обмена;
  - завершающий `\n` и границы строк обрабатываются по спецификации.

## Критерии готовности

- [ ] В репозитории создан минимальный Rider-plugin проект на Kotlin.
- [ ] Добавлен и используется Gradle Wrapper.
- [ ] Action копирования реализована по спецификации `T0002`.
- [ ] Проект проходит минимальную сборочную проверку.
- [ ] Подготовлен OUT-отчёт как вход для TESTER.

## Артефакты OUT

- отчёт CODER: `.agents-runtime/MAIL/CODER/OUT/T0003_20260410-0205_CODER_bootstrap-rider-plugin-and-implement-copy-action_report.md`

## Рекомендованная модель и режим

- model: gpt-5
- reasoning effort: medium
- комментарий: при проблемах с JetBrains plugin bootstrap или Gradle-конфигурацией повышать до high
