# TASK — тикет

Role: CODER
TicketId: T0009
CreatedAt: 2026-04-11 18:08

## Контекст

После тикета `T0008` build-конфигурация мигрирована на `IntelliJ Platform Gradle Plugin 2.x`, и `buildPlugin` проходит успешно. Но установка ZIP в реальный Rider пользователя проваливается с ошибкой совместимости:

`Plugin 'Rider Copy Format Plugin' (version '0.1.0') is not compatible with the current version of the IDE, because it requires build 243.* or older but the current build is RD-261.22158.394`

Значит, текущий диапазон `sinceBuild/untilBuild` всё ещё ограничен веткой `243.*` и не соответствует фактической целевой IDE пользователя (`261.*`, Rider `2025+`).

Нужно привести конфигурацию совместимости и build-only зависимости к актуальному состоянию: взять максимально свежие доступные версии библиотек/плагинов в рамках минимально достаточного и поддерживаемого решения, чтобы проект собирался и устанавливался в Rider `261.*`.

## Scope

- Обновить конфигурацию совместимости плагина под Rider `261.*`.
- При необходимости обновить build-only версии плагинов/зависимостей до максимально свежих доступных и уместных для этой задачи.
- Сохранить текущую функциональность плагина без изменений по поведению.
- Сохранить сборку через `.\gradlew.bat buildPlugin`.
- Добиться того, чтобы собранный ZIP не отбрасывался Rider `RD-261.22158.394` по метаданным совместимости.

## Не делать

- Не менять бизнес-логику action, hotkey, формат буфера и UX, если это не требуется напрямую для совместимости.
- Не расползаться в публикацию, Marketplace, CI, release-процесс и прочую внешнюю инфраструктуру.
- Не менять файлы вне `D:\.DEV\JetBrainsPlagins`.
- Не занижать версии искусственно; ориентироваться на самые свежие доступные версии, которые минимально и корректно решают задачу совместимости.

## Входные артефакты

- отчёт CODER: `.agents-runtime/MAIL/CODER/OUT/T0008_20260410-2343_CODER_migrate-build-to-intellij-platform-gradle-plugin-2x_report.md`
- текущий build-script: `build.gradle.kts`
- параметры платформы: `gradle.properties`
- ошибка установки в Rider `RD-261.22158.394` из пользовательского прогона

## Шаги

1. Проверить текущие значения `sinceBuild`, `untilBuild`, `platformVersion` и связанные build-only версии.
2. Обновить совместимость под Rider `261.*`.
3. При необходимости обновить build-only версии до максимально свежих доступных, если это помогает корректной сборке/совместимости.
4. Собрать плагин через `.\gradlew.bat buildPlugin`.
5. Зафиксировать в отчёте, какие версии выбраны и почему.

## Проверки

- Проверить, что `buildPlugin` проходит успешно.
- Проверить, что метаданные плагина больше не ограничены `243.*`, если это мешает Rider `261.*`.
- Проверить, что итоговая конфигурация совместима с Rider build `RD-261.22158.394`.
- Явно перечислить обновлённые build-only версии и итоговый диапазон совместимости.

## Критерии готовности

- [ ] Конфигурация совместимости обновлена под Rider `261.*`.
- [ ] Build-only версии приведены к актуальному и поддерживаемому состоянию.
- [ ] `buildPlugin` проходит успешно.
- [ ] Подготовлен OUT-отчёт с итоговыми версиями и результатами проверок.

## Артефакты OUT

- отчёт CODER: `.agents-runtime/MAIL/CODER/OUT/T0009_20260411-1808_CODER_expand-rider-compatibility-to-261-and-refresh-build-versions_report.md`

## Рекомендованная модель и режим

- model: GPT-5.3-Codex
- reasoning effort: medium
- комментарий: нужен точный build/migration-патч без расползания в лишние изменения
