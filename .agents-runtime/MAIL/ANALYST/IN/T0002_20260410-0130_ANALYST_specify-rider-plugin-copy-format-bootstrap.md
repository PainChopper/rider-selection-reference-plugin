# TASK — тикет

Role: ANALYST
TicketId: T0002
CreatedAt: 2026-04-10 01:30

## Контекст
Нужно подготовить спецификацию для отдельного Rider-плагина, который разрабатывается только в каталоге `D:\!_DEV_!\JetBrainsPlagins` и не должен требовать изменений вне этого каталога.

Целевая функция плагина: из редактора Rider копировать текст в формате:
1) `<path>:<lineStart>-<lineEnd>`
2) выделенный текст

Результат ANALYST нужен как вход для следующего тикета CODER на реализацию плагина на Java + Gradle.

## Scope
- Зафиксировать точную спецификацию выходного формата для операции копирования.
- Описать правила определения `path`, `lineStart`, `lineEnd`.
- Описать поведение для пустого выделения, выделения одной строки, нескольких строк, частичного выделения строк и выделения с завершающим переводом строки.
- Описать edge-cases для разных типов файлов/вкладок Rider, если они влияют на формат результата.
- Зафиксировать нефункциональные ограничения для следующего шага реализации:
  - разработка только в `D:\!_DEV_!\JetBrainsPlagins`
  - без изменений вне `D:\!_DEV_!\JetBrainsPlagins`

## Не делать
- Не реализовывать плагин.
- Не создавать Gradle-проект и не писать код.
- Не расширять функциональность сверх формата копирования и необходимых edge-cases.

## Шаги
1. Прочитать исходный тикет LEAD: `.agents-runtime/MAIL/LEAD/IN/T0001_20260410-0108_LEAD_rider-plugin-bootstrap.md`.
2. Подготовить спецификацию пользовательского результата копирования.
3. Явно перечислить edge-cases и ожидаемое поведение для каждого.
4. Зафиксировать допущения и открытые вопросы только если без них нельзя однозначно завершить спецификацию.

## Проверки
- Проверить, что спецификация однозначно отвечает, какой текст должен попасть в буфер обмена.
- Проверить, что в результате явно указаны ограничения по каталогам и запрет на изменения вне `D:\!_DEV_!\JetBrainsPlagins`.
- Проверить, что OUT-файл назван по формату и создан в `.agents-runtime/MAIL/ANALYST/OUT/`.

## Критерии готовности
- [ ] Есть однозначная спецификация формата копирования.
- [ ] Edge-cases перечислены и снабжены ожидаемым поведением.
- [ ] Зафиксированы рабочие ограничения по каталогам.
- [ ] OUT-файл готов как вход для CODER.

## Артефакты OUT
- отчёт ANALYST: `.agents-runtime/MAIL/ANALYST/OUT/T0002_20260410-0130_ANALYST_specify-rider-plugin-copy-format-bootstrap_report.md`

## Следующий маршрут
- После этого тикета LEAD готовит тикет для CODER на реализацию Rider-плагина на Java + Gradle.
