# .agents-runtime

Mutable process/runtime layer.
Нормативный source of truth лежит в `AGENTS.md` и `.agents/**`.
State: `00_STATE.md`; plans: `PLANS/`; run artifacts: `RUNLOGS/<ticket-id>/`; process files: `MAIL/<ROLE>/{IN,OUT}/`.
